{
  "name": "browserify-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "mkdir -p public/build && touch public/build/app.min.js",
    "watch": "watchify src/main.js -o 'exorcist public/build/app.min.js.map > public/build/app.min.js' -d"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "watchify": "^3.3.1"
  },
  "dependencies": {
    "exorcist": "^0.4.0",
    "jquery": "^1.11.3"
  }
}
