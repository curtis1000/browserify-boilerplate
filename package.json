{
  "name": "browserify-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "mkdir -p public/build && touch public/build/app.min.js",
    "watch": "watchify src/main.js -o 'uglifyjs -cm > public/build/app.min.js'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "watchify": "^3.3.1",
    "uglifyjs": "^2.4.10"
  },
  "dependencies": {
    "jquery": "^1.11.3"
  }
}
