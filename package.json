{
  "name": "browserify-boilerplate",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "mkdir public/build && touch public/build/app.min.js",
    "build": "browserify src/main.js | uglifyjs -o public/build/app.min.js",
    "watch": "nodemon -e js -w src -x 'npm run build'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^11.0.1",
    "nodemon": "^1.4.1",
    "uglifyjs": "^2.4.10"
  }
}
